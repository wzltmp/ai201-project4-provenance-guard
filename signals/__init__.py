"""Detection signals for Provenance Guard.

Each signal is a standalone function returning ``p_ai`` in [0, 1] (probability the text is
AI-generated). Milestone 3 ships Signal 1 (the LLM judge); Signal 2 (stylometry) lands in M4.
"""
